"use client";import Link from"next/link";import{useEffect,useState}from"react";
type Bundle={id:string;slug:string;name:string;description:string;discountPercent:number;subtotal:number;discountValue:number;total:number;items:{quantity:number;product:{productId:string;name:string;category:string}}[]};
export default function Bundles(){const[data,setData]=useState<Bundle[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{fetch("/api/bundles").then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error||"Gagal memuat bundles");return x}).then(x=>setData(x.bundles||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <main className="container-ns py-16"><div className="eyebrow">STORE / BUNDLES</div><h1 className="text-5xl font-black">Creative Bundles</h1><p className="muted max-w-2xl mt-4">Paket beberapa produk digital dengan harga gabungan yang dikelola dari database.</p>{loading&&<p className="muted mt-8">Memuat bundles...</p>}{error&&<div className="card p-5 mt-8 text-red-700" role="alert">{error}</div>}<div className="grid-auto mt-10">{data.map(b=><article className="card p-6" key={b.id}><div className="text-xs text-violet-700">{b.discountPercent}% BUNDLE DISCOUNT</div><h2 className="text-2xl font-black mt-2">{b.name}</h2><p className="text-neutral-500 mt-3">{b.description}</p><div className="stack mt-5">{b.items.map(i=><div className="text-sm flex justify-between gap-3" key={i.product.productId}><span>{i.product.name}</span><span>×{i.quantity}</span></div>)}</div><div className="mt-6"><del className="text-sm text-neutral-400">{b.subtotal.toLocaleString("id-ID")} IDR</del><div className="text-3xl font-black">{b.total.toLocaleString("id-ID")} IDR</div></div><Link className="btn btn-primary mt-6 w-full" href="/store">Browse Products</Link></article>)}</div></main>}

"use client";import{FormEvent,useState}from"react";import{useRouter,useSearchParams}from"next/navigation";
export default function Login(){const router=useRouter(),search=useSearchParams(),next=search.get("next");const[mode,setMode]=useState<"login"|"register">("login"),[name,setName]=useState(""),[username,setUsername]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setMessage("");setError("");setLoading(true);try{const endpoint=mode==="login"?"/api/auth/login":"/api/auth/register";const body=mode==="login"?{email,password}:{name,username,email,password};const r=await fetch(endpoint,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const x=await r.json();if(!r.ok)throw new Error(x.error||"Permintaan gagal");setMessage(mode==="login"?"Login berhasil. Mengarahkan...":"Akun berhasil dibuat. Mengarahkan...");setTimeout(()=>router.push(next?.startsWith("/")?next:"/dashboard"),350)}catch(err){setError(err instanceof Error?err.message:"Permintaan gagal")}finally{setLoading(false)}}return <main className="page"><div className="card" style={{maxWidth:520,margin:"auto"}}><div className="eyebrow">ACCOUNT</div><h1>{mode==="login"?"Login":"Buat Akun"}</h1><form onSubmit={submit} className="stack">{mode==="register"&&<><input required maxLength={120} placeholder="Nama" value={name} onChange={e=>setName(e.target.value)}/><input required minLength={3} maxLength={40} pattern="[a-z0-9_]+" placeholder="Username" value={username} onChange={e=>setUsername(e.target.value)}/></>}<input required type="email" maxLength={160} placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input required minLength={8} type="password" placeholder="Password (min. 8 karakter)" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn btn-primary" type="submit" disabled={loading}>{loading?"Memproses...":mode==="login"?"Masuk":"Daftar"}</button></form>{message&&<p className="muted" role="status">{message}</p>}{error&&<p className="text-red-700" role="alert">{error}</p>}<button type="button" className="btn btn-ghost w-full mt-4" onClick={()=>{setMode(mode==="login"?"register":"login");setMessage("");setError("")}}>{mode==="login"?"Belum punya akun? Daftar":"Sudah punya akun? Login"}</button></div></main>}

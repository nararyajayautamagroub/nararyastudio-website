"use client"; import {FormEvent,useState} from "react";
export default function Login(){const [message,setMessage]=useState("");function submit(e:FormEvent){e.preventDefault();setMessage("UI login siap. Hubungkan session/auth provider untuk autentikasi production.");}return <main className="page"><div className="card" style={{maxWidth:520,margin:"auto"}}><div className="eyebrow">ACCOUNT</div><h1>Login</h1><form onSubmit={submit} className="stack"><input required type="email" placeholder="Email"/><input required type="password" placeholder="Password"/><button className="button" type="submit">Masuk</button></form>{message&&<p className="muted">{message}</p>}</div></main>}
import{NextResponse}from"next/server";import{randomBytes}from"node:crypto";
function safeNext(value:string|null){return value&&value.startsWith("/")&&!value.startsWith("//")?value:"/dashboard"}
export async function GET(req:Request){const clientId=process.env.GOOGLE_CLIENT_ID,redirectUri=process.env.GOOGLE_REDIRECT_URI||new URL("/api/auth/google/callback",process.env.NEXT_PUBLIC_SITE_URL||new URL(req.url).origin).toString();if(!clientId)return NextResponse.json({error:"Google login belum dikonfigurasi."},{status:503});const next=safeNext(new URL(req.url).searchParams.get("next")),state=randomBytes(32).toString("hex");const url=new URL("https://accounts.google.com/o/oauth2/v2/auth");url.searchParams.set("client_id",clientId);url.searchParams.set("redirect_uri",redirectUri);url.searchParams.set("response_type","code");url.searchParams.set("scope","openid email profile");url.searchParams.set("state",state);url.searchParams.set("access_type","online");const res=NextResponse.redirect(url);res.cookies.set({name:"ns_oauth_state",value:`${state}:${encodeURIComponent(next)}`,httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:600});return res}

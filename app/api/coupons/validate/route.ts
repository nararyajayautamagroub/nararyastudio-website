import{NextResponse}from"next/server";import{safeText}from"@/lib/security";import{findValidCoupon,calculateCouponDiscount}from"@/lib/coupons";
export async function POST(req:Request){try{const body=await req.json(),code=safeText(body.code,64).toUpperCase(),subtotal=Number(body.subtotal);if(!code||!Number.isFinite(subtotal)||subtotal<0)return NextResponse.json({error:"Coupon request tidak valid."},{status:400});const coupon=await findValidCoupon(code,subtotal);if(!coupon)return NextResponse.json({valid:false,error:"Coupon tidak valid."},{status:400});return NextResponse.json({valid:true,code:coupon.code,discount:calculateCouponDiscount(coupon,subtotal),minSubtotal:coupon.minSubtotal})}catch{return NextResponse.json({error:"Gagal memvalidasi coupon."},{status:500})}}

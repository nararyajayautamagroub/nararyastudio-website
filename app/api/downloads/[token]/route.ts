import{NextResponse}from"next/server";import{db}from"@/lib/db";import{hashToken}from"@/lib/security";
export async function GET(_:Request,{params}:{params:Promise<{token:string}>}){try{const{token}=await params;const record=await db.downloadLog.findFirst({where:{tokenHash:hashToken(token)},include:{product:true,order:true}});if(!record)return NextResponse.json({error:"Link download tidak valid."},{status:404});if(record.expiresAt<=new Date())return NextResponse.json({error:"Link download sudah kedaluwarsa."},{status:410});if(record.maxDownloads!==null&&record.count>=record.maxDownloads)return NextResponse.json({error:"Batas download tercapai."},{status:410});if(!record.order||record.order.paymentStatus!=="PAID")return NextResponse.json({error:"Pembayaran belum terverifikasi."},{status:403});if(!record.product.filePath)return NextResponse.json({error:"File belum tersedia."},{status:404});const next=await db.downloadLog.update({where:{id:record.id},data:{count:{increment:1}}});if(record.maxDownloads!==null&&next.count>record.maxDownloads){return NextResponse.json({error:"Batas download tercapai."},{status:410})}return NextResponse.redirect(new URL(record.product.filePath,process.env.STORAGE_BASE_URL||new URL("https://example.invalid").origin))}catch{return NextResponse.json({error:"Download gagal."},{status:500})}}

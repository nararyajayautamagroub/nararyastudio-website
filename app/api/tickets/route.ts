import {NextResponse} from "next/server"; import {db} from "@/lib/db"; import {safeText} from "@/lib/security";
export async function POST(req:Request){try{const b=await req.json();if(!b.userId||!b.subject||!b.message)return NextResponse.json({error:"Data ticket belum lengkap."},{status:400});const t=await db.ticket.create({data:{ticketId:"NS-TKT-"+Date.now().toString().slice(-8),userId:safeText(b.userId,80),subject:safeText(b.subject,160),category:safeText(b.category,80)||"GENERAL",message:safeText(b.message,5000)}});return NextResponse.json({ticketId:t.ticketId,status:t.status},{status:201})}catch{return NextResponse.json({error:"Ticket gagal dibuat."},{status:500})}}
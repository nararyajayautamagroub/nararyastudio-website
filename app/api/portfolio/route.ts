import{NextResponse}from"next/server";import{db}from"@/lib/db";
export async function GET(req:Request){try{const{searchParams}=new URL(req.url),category=(searchParams.get("category")||"").trim().slice(0,80),q=(searchParams.get("q")||"").trim().slice(0,100);const items=await db.portfolioEntry.findMany({where:{published:true,...(category?{category}:{}),...(q?{OR:[{title:{contains:q,mode:"insensitive"}},{description:{contains:q,mode:"insensitive"}}]}:{})},orderBy:[{featured:"desc"},{createdAt:"desc"}],select:{id:true,slug:true,title:true,category:true,description:true,image:true,services:true,year:true,featured:true,createdAt:true}});return NextResponse.json({items})}catch{return NextResponse.json({error:"Gagal memuat portfolio."},{status:500})}}

import{NextResponse}from"next/server";import{createHmac,timingSafeEqual}from"node:crypto";import{db}from"@/lib/db";import{safeText}from"@/lib/security";
function validSignature(body:string,signature:string,secret:string){const expected=createHmac("sha256",secret).update(body).digest("hex");try{const a=Buffer.from(expected,"utf8"),b=Buffer.from(signature.replace(/^sha256=/i,""),"utf8");return a.length===b.length&&timingSafeEqual(a,b)}catch{return false}}
export async function POST(req:Request){const secret=process.env.PAYMENT_WEBHOOK_SECRET;if(!secret)return NextResponse.json({error:"Webhook belum dikonfigurasi."},{status:503});const raw=await req.text();const signature=req.headers.get("x-webhook-signature")||"";if(!validSignature(raw,signature,secret))return NextResponse.json({error:"Unauthorized"},{status:401});try{const body=JSON.parse(raw) as{eventId?:unknown;orderId?:unknown;status?:unknown;amount?:unknown};const eventId=safeText(body.eventId,120);const orderCode=safeText(body.orderId,80);const status=safeText(body.status,30).toUpperCase();const amount=Number(body.amount);if(!eventId||!orderCode||!["PAID","FAILED","EXPIRED","REFUNDED"].includes(status))return NextResponse.json({error:"Payload webhook tidak valid."},{status:400});const existing=await db.paymentEvent.findUnique({where:{eventId}});if(existing)return NextResponse.json({received:true,duplicate:true});const order=await db.order.findUnique({where:{orderId:orderCode}});if(!order)return NextResponse.json({error:"Order tidak ditemukan."},{status:404});if(Number.isFinite(amount)&&amount!==order.total)return NextResponse.json({error:"Nominal pembayaran tidak sesuai."},{status:400});const paymentStatus=status as "PAID"|"FAILED"|"EXPIRED"|"REFUNDED";const orderStatus=paymentStatus==="PAID"?"PAID":paymentStatus==="REFUNDED"?"REFUNDED":paymentStatus==="FAILED"?"CANCELLED":"CANCELLED";await db.$transaction([db.paymentEvent.create({data:{eventId,orderId:order.id,status:paymentStatus,amount:Number.isFinite(amount)?amount:null,payload:body}}),db.order.update({where:{id:order.id},data:{paymentStatus,status:orderStatus}})]);return NextResponse.json({received:true,duplicate:false})}catch{return NextResponse.json({error:"Webhook gagal diproses."},{status:500})}}

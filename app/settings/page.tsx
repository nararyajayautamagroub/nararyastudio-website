"use client";import{FormEvent,useEffect,useState}from"react";import{useI18n}from"@/components/i18n-provider";
export default function Settings(){const{t,locale,setLocale,names,locales,theme,setTheme}=useI18n();const[name,setName]=useState(""),[email,setEmail]=useState(""),[username,setUsername]=useState(""),[googleConnected,setGoogleConnected]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false);useEffect(()=>{fetch("/api/settings").then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error||"Unauthorized");setName(x.settings.name);setEmail(x.settings.email);setUsername(x.settings.username);setGoogleConnected(x.settings.googleConnected);if(locales.includes(x.settings.locale))setLocale(x.settings.locale);if(["system","light","dark"].includes(x.settings.theme))setTheme(x.settings.theme)}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);async function save(e:FormEvent){e.preventDefault();setSaving(true);setError("");setMessage("");try{const r=await fetch("/api/settings",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({name,locale,theme})});const x=await r.json();if(!r.ok)throw new Error(x.error||"Pengaturan gagal disimpan");setMessage(t("save")+" ✓");setLocale(x.settings.locale);setTheme(x.settings.theme)}catch(e){setError(e instanceof Error?e.message:"Pengaturan gagal disimpan")}finally{setSaving(false)}}if(loading)return <main className="page muted">Memuat pengaturan...</main>;if(error&&!name)return <main className="page"><section className="card p-8 max-w-2xl mx-auto"><h1 className="text-4xl font-black">{t("settings")}</h1><p className="text-red-700 mt-4">{error}</p></section></main>;return <main className="page"><div className="eyebrow">ACCOUNT</div><h1>{t("settings")}</h1><form onSubmit={save} className="card p-6 max-w-3xl mt-8 stack"><div><label className="text-sm font-semibold">Nama</label><input className="mt-2" maxLength={120} required value={name} onChange={e=>setName(e.target.value)}/></div><div className="grid md:grid-cols-2 gap-4"><div><label className="text-sm font-semibold">Username</label><input className="mt-2" value={username} disabled/></div><div><label className="text-sm font-semibold">Email</label><input className="mt-2" value={email} disabled/></div></div><div><label className="text-sm font-semibold block">{t("language")}</label><select className="mt-2" value={locale} onChange={e=>setLocale(e.target.value as typeof locales[number])}>{locales.map(code=><option key={code} value={code}>{names[code]}</option>)}</select></div><div><label className="text-sm font-semibold block">{t("theme")}</label><select className="mt-2" value={theme} onChange={e=>setTheme(e.target.value as typeof theme)}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></div><div className="card p-4 bg-violet-50 dark:bg-neutral-900"><b>Google Account</b><p className="text-sm text-neutral-600 dark:text-neutral-300 mt-1">{googleConnected?"Google sudah terhubung ke akun ini.":"Belum terhubung."}</p></div><button className="btn btn-primary" disabled={saving} type="submit">{saving?"Menyimpan...":t("save")}</button>{message&&<p role="status" className="text-emerald-700">{message}</p>}{error&&<p role="alert" className="text-red-700">{error}</p>}</form></main>}

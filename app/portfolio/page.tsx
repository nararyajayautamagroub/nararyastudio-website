"use client";import{useEffect,useState}from"react";type Item={id:string;slug:string;title:string;category:string;description:string;image:string|null;services:unknown;year:number|null;featured:boolean};export default function Portfolio(){const[data,setData]=useState<Item[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/portfolio").then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error||"Gagal memuat portfolio");return x}).then(x=>setData(x.items||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <main className="container-ns py-16"><p className="text-xs text-neutral-500">PORTFOLIO</p><h1 className="text-5xl font-black mt-2">Selected work</h1>{loading&&<p className="muted mt-8">Memuat portfolio...</p>}{error&&<div role="alert" className="card p-5 mt-8 text-red-700">{error}</div>}<div className="grid-auto mt-10">{data.map(item=><article className="card overflow-hidden" key={item.id}><div className="aspect-video bg-neutral-900 flex items-end p-6 text-neutral-600 text-5xl font-black">{item.category?.[0]||"N"}</div><div className="p-6"><div className="text-xs text-neutral-500">{item.category}{item.year?" · "+item.year:""}</div><h2 className="text-xl font-bold mt-1">{item.title}</h2><p className="text-sm text-neutral-500 mt-2">{item.description}</p>{Array.isArray(item.services)&&<div className="flex flex-wrap gap-2 mt-4">{item.services.map((s)=><span key={String(s)} className="text-xs rounded-full bg-violet-50 px-3 py-1 text-violet-800">{String(s)}</span>)}</div>}</div></article>)}</div>{!loading&&!error&&!data.length&&<div className="card p-8 mt-8 text-neutral-500">Portfolio belum tersedia.</div>}</main>}
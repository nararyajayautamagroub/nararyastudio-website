"use client";import{useEffect,useState}from"react";type Item={id:string;slug:string;title:string;category:string;summary:string;description:string;software:unknown;services:unknown;status:string;coverImage:string|null;completedAt:string|null};export default function Projects(){const[data,setData]=useState<Item[]>([]),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/projects").then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error||"Gagal memuat projects");return x}).then(x=>setData(x.items||[])).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <main className="container-ns py-16"><p className="text-xs text-neutral-500">PROJECTS / CASE STUDY</p><h1 className="text-5xl font-black mt-2">Project showcase</h1>{loading&&<p className="muted mt-8">Memuat projects...</p>}{error&&<div role="alert" className="card p-5 mt-8 text-red-700">{error}</div>}<div className="stack mt-10">{data.map(item=><article className="card p-8" key={item.id}><div className="text-xs text-neutral-500">CASE STUDY · {item.category}</div><h2 className="text-3xl font-black mt-2">{item.title}</h2><p className="text-neutral-500 mt-4 max-w-2xl">{item.summary}</p><p className="text-neutral-600 mt-4 max-w-3xl">{item.description}</p><div className="grid md:grid-cols-3 gap-4 mt-8"><div className="bg-neutral-900 rounded-xl p-4 text-white"><div className="text-xs text-neutral-400">Software</div><div className="font-semibold mt-1">{Array.isArray(item.software)?item.software.join(" · "):"Tidak ditentukan"}</div></div><div className="bg-neutral-900 rounded-xl p-4 text-white"><div className="text-xs text-neutral-400">Services</div><div className="font-semibold mt-1">{Array.isArray(item.services)?item.services.join(" · "):"Tidak ditentukan"}</div></div><div className="bg-neutral-900 rounded-xl p-4 text-white"><div className="text-xs text-neutral-400">Status</div><div className="font-semibold mt-1">{item.status}</div></div></div></article>)}</div>{!loading&&!error&&!data.length&&<div className="card p-8 mt-8 text-neutral-500">Project belum tersedia.</div>}</main>}
"use client";import Link from"next/link";import{use,useEffect,useState}from"react";import{formatIDR}from"@/lib/data";import{addToCart}from"@/lib/client-cart";
type P={id:string;productId:string;name:string;category:string;price:number;discount:number;description:string;version:string;compatibility?:string|null;requirements?:string|null;license?:string|null;author?:string|null;changelog?:string|null};
export default function ProductDetail({params}:{params:Promise<{id:string}>}){const{id}=use(params);const[p,setP]=useState<P|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(true),[added,setAdded]=useState(false);useEffect(()=>{const c=new AbortController();setLoading(true);fetch("/api/products/"+encodeURIComponent(id),{signal:c.signal}).then(async r=>{const x=await r.json();if(!r.ok)throw new Error(x.error||"Produk tidak ditemukan");return x}).then(x=>setP(x.product)).catch(e=>{if(e.name!=="AbortError")setError(e.message||"Produk tidak ditemukan")}).finally(()=>setLoading(false));return()=>c.abort()},[id]);if(loading)return <main className="container-ns py-16 muted">Memuat produk...</main>;if(error||!p)return <main className="container-ns py-16"><div className="card p-8" role="alert">{error||"Produk tidak ditemukan."}<div className="mt-5"><Link href="/store" className="btn">Kembali ke Store</Link></div></div></main>;const discount=Math.min(100,Math.max(0,p.discount));const finalPrice=Math.max(0,p.price-Math.round(p.price*discount/100));const add=()=>{addToCart(p.productId);setAdded(true);setTimeout(()=>setAdded(false),1800)};return <main className="container-ns py-16"><Link href="/store" className="text-sm text-violet-800">← Kembali ke Store</Link><div className="grid md:grid-cols-2 gap-10 mt-8"><div className="card aspect-square bg-violet-50 flex items-center justify-center text-8xl font-black text-violet-200">{p.category?.[0]||"N"}</div><div><div className="text-xs text-violet-700 font-semibold">{p.productId} · {p.category}</div><h1 className="text-4xl font-black mt-2">{p.name}</h1><p className="text-neutral-600 mt-5 leading-7">{p.description}</p><div className="text-3xl font-black mt-8">{formatIDR(finalPrice)} {discount>0&&<del className="text-base text-neutral-400 ml-2">{formatIDR(p.price)}</del>}</div><div className="grid grid-cols-2 gap-3 mt-8 text-sm"><div className="card p-4"><b>Version</b><div className="text-neutral-500 mt-1">{p.version}</div></div><div className="card p-4"><b>Author</b><div className="text-neutral-500 mt-1">{p.author||"NARARYA STUDIO"}</div></div><div className="card p-4"><b>License</b><div className="text-neutral-500 mt-1">{p.license||"Digital license"}</div></div><div className="card p-4"><b>Compatibility</b><div className="text-neutral-500 mt-1">{p.compatibility||"Tidak ditentukan"}</div></div></div>{p.requirements&&<div className="card p-4 mt-3 text-sm"><b>Requirements</b><p className="text-neutral-500 mt-1 whitespace-pre-line">{p.requirements}</p></div>}{p.changelog&&<div className="card p-4 mt-3 text-sm"><b>Changelog</b><p className="text-neutral-500 mt-1 whitespace-pre-line">{p.changelog}</p></div>}<div className="flex gap-3 mt-8"><button type="button" className="btn btn-primary flex-1" onClick={add}>{added?"✓ Added":"Add to Cart"}</button><Link href="/cart" className="btn btn-ghost flex-1">View Cart</Link></div></div></div></main>}

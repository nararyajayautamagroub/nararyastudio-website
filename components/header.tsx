"use client"; import Link from "next/link"; import {useState} from "react"; import {Menu,ShoppingBag,X} from "lucide-react";
const links=[["Store","/store"],["Design","/design"],["Request","/request"],["Portfolio","/portfolio"],["Projects","/projects"]];
export function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-neutral-800/80 bg-black/85 backdrop-blur"><div className="container-ns h-16 flex items-center justify-between"><Link href="/" className="font-black tracking-tight text-lg">NARARYA STUDIO</Link><nav className="hidden md:flex items-center gap-6 text-sm text-neutral-300">{links.map(([n,h])=><Link key={h} href={h} className="hover:text-white">{n}</Link>)}</nav><div className="flex items-center gap-2"><Link href="/cart" className="btn btn-ghost p-3" aria-label="Keranjang"><ShoppingBag size={18}/></Link><button className="md:hidden btn btn-ghost p-3" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X size={18}/>:<Menu size={18}/>}</button></div></div>{open&&<nav className="md:hidden border-t border-neutral-800 p-4 grid gap-2">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} key={h} href={h} className="p-3 rounded-xl hover:bg-neutral-900">{n}</Link>)}</nav>}</header>}
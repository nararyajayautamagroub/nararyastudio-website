"use client";import{createContext,useContext,useEffect,useState}from"react";import{LOCALES,localeNames,rtlLocales,translate,type Locale}from"@/lib/locale";
type Ctx={locale:Locale;setLocale:(value:Locale)=>void;t:(key:string)=>string;locales:typeof LOCALES;names:Record<Locale,string>};
const I18nContext=createContext<Ctx|null>(null);
export function I18nProvider({children,initialLocale="id"}:{children:React.ReactNode;initialLocale?:Locale}){const[locale,setLocaleState]=useState<Locale>(initialLocale);useEffect(()=>{const saved=window.localStorage.getItem("ns_locale") as Locale|null;if(saved&&LOCALES.includes(saved))setLocaleState(saved)},[]);useEffect(()=>{window.localStorage.setItem("ns_locale",locale);document.cookie=`ns_locale=${locale}; Path=/; Max-Age=31536000; SameSite=Lax`;document.documentElement.lang=locale;document.documentElement.dir=rtlLocales.includes(locale)?"rtl":"ltr"},[locale]);const setLocale=(value:Locale)=>setLocaleState(value);return <I18nContext.Provider value={{locale,setLocale,t:(key)=>translate(locale,key),locales:LOCALES,names:localeNames}}>{children}</I18nContext.Provider>}
export function useI18n(){const value=useContext(I18nContext);if(!value)throw new Error("useI18n must be used inside I18nProvider");return value}

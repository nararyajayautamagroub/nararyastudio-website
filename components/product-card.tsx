import Link from "next/link"; import {Product,formatIDR} from "@/lib/data";
export function ProductCard({p}:{p:Product}){return <article className="card overflow-hidden"><div className="aspect-[4/3] bg-neutral-900 flex items-center justify-center text-neutral-600 text-5xl font-black">{p.category.slice(0,1)}</div><div className="p-5"><div className="text-xs text-neutral-500">{p.category} · V{p.version}</div><h3 className="font-bold text-lg mt-1">{p.name}</h3><p className="text-sm text-neutral-400 mt-2 line-clamp-2">{p.description}</p><div className="mt-5 flex items-center justify-between gap-2"><span className="font-bold">{formatIDR(p.price)}</span><Link className="btn btn-primary text-sm" href={"/store/"+p.id}>Detail</Link></div></div></article>}